# Copy to .env for docker compose variable overrides (optional)
API_PORT=8030
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@miu.local
PGADMIN_DEFAULT_PASSWORD=admin
JWT_SECRET=change-me-in-production-use-long-random-string
CORS_ORIGINS=http://localhost:5173
SEED_ON_START=true
ENVIRONMENT=development
